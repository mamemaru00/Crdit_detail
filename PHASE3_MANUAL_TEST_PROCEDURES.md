# Phase 3: 手動テスト手順書（パターンB/C）

**作成日**: 2026-02-04
**対象**: Phase 3テスト＆検証 - パターンB/C
**ステータス**: 実施可能
**注意**: 発見された4件の問題の影響を受けないテストケース

---

## 📋 テスト実施概要

### 実施可能な理由

パターンB（未登録なし）とパターンC（ユーザー拒否）は、`/gpt/confirm`エンドポイントを使用しないため、発見された以下の問題の影響を受けません：

- ❌ 問題1: `session_store.get_session()` メソッド不存在
- ❌ 問題2: リクエストキー名不一致 (`confirmed_data` vs `classifications`)
- ❌ 問題3: データキー名不一致 (`store_name` vs `store`)
- ❌ 問題4: `session_store.delete_session()` メソッド不存在

### 前提条件

1. ✅ Docker環境が起動していること
   ```bash
   docker ps
   # CONTAINER ID   IMAGE              PORTS
   # ba8f92509ddf   nginx:alpine       0.0.0.0:5000->80/tcp
   # 04daf537dfca   crdit_detail-web   5000/tcp
   ```

2. ✅ ブラウザで `http://localhost:5000` にアクセス可能

3. ✅ テスト用CSVファイルが準備済み
   - パターンB用: すべての店舗がマッピング登録済みのCSV
   - パターンC用: 未登録店舗を含むCSV

4. ✅ Googleスプレッドシートの準備
   - スプレッドシートIDが判明していること
   - サービスアカウントに編集権限があること
   - 該当年のシートが存在すること（例: 2025年シート）

---

## 🧪 テストケース1: パターンB（未登録なし）

### 目的

すべての店舗がマッピング登録済みの場合、ChatGPT分類フローを経由せずに即座にGoogle Sheetsに反映されることを確認する。

### テストデータ準備

#### 1. マッピングデータの事前登録

マッピング管理画面（`http://localhost:5000/mapping`）で以下の店舗を登録：

| 店舗名 | マッチタイプ | カテゴリ | 列 | 優先度 |
|-------|------------|---------|-----|--------|
| イオン | contains | 日用品費 | D列 | 1 |
| セブンイレブン | contains | 外食費 | C列 | 1 |
| ファミリーマート | contains | 外食費 | C列 | 1 |

#### 2. テスト用CSVファイル作成

`test_pattern_b.csv`（Shift_JIS、内容例）:

```csv
ご利用日,ご利用店名,ご利用金額
260115,イオン,1500
260116,セブンイレブン,500
260117,ファミリーマート,300
```

### テスト手順

#### Step 1: 初期画面表示確認

1. ブラウザで `http://localhost:5000` にアクセス
2. **期待結果**:
   - ページタイトル: 「イオンカード明細取込システム」
   - Step 1: CSVファイル選択エリアが表示
   - Step 2～5は非表示（`d-none`クラス）

#### Step 2: CSVファイルアップロード

1. 「ファイルを選択」ボタンをクリック
2. `test_pattern_b.csv` を選択
3. 「プレビュー表示」ボタンをクリック
4. **期待結果**:
   - プログレスバーが表示される
   - 数秒後、プログレスバーが非表示になる
   - Bootstrap Toastで「プレビューを表示しました」が表示される（5秒後に自動消滅）

#### Step 3: プレビュー表示確認

1. **期待結果**:
   - Step 2: プレビューエリアが表示される
   - テーブルに3行のデータが表示される:
     - 2026/01/15, イオン, 1,500円
     - 2026/01/16, セブンイレブン, 500円
     - 2026/01/17, ファミリーマート, 300円
   - プレビューエリアまで自動スクロールされる

#### Step 4: 設定確認

1. **期待結果**:
   - Step 3: 設定エリアが表示される
   - 「スプレッドシートID」入力欄にデフォルト値が入力済み
   - 「対象年」ドロップダウンに2025年が選択済み

2. スプレッドシートIDを確認（必要に応じて変更）

#### Step 5: 取込実行

1. Step 4: 取込実行エリアの「取込実行」ボタンをクリック
2. **期待結果**:
   - 確認ダイアログが表示される
   - メッセージ: 「2025年のスプレッドシートにデータを反映します。よろしいですか？」

3. 「OK」をクリック
4. **期待結果**:
   - プログレスバーが表示される
   - 数秒後（Google Sheets API呼び出し完了後）、プログレスバーが非表示になる
   - **リダイレクトが発生する**（`http://localhost:5000/?message=success`）

#### Step 6: 成功メッセージ確認

1. **期待結果**:
   - トップ画面にリダイレクトされる
   - ページ上部にBootstrap Toastが表示される:
     - 背景色: 緑（`text-bg-success`）
     - アイコン: チェックマーク（`bi-check-circle-fill`）
     - メッセージ: 「取込処理が正常に完了しました」
     - 5秒後に自動消滅

2. **未登録店舗リストが表示されない**
   - Step 5エリアは表示されない（または未登録店舗セクションが非表示）

#### Step 7: Google Sheets確認

1. Googleスプレッドシートを開く
2. 2025年シートを選択
3. **期待結果**:
   - 1月行のD列（日用品費）に1,500円が加算されている
   - 1月行のC列（外食費）に800円（500 + 300）が加算されている
   - 他の月・列は変更されていない

### 判定基準

- ✅ **合格**: すべてのステップが期待結果通りに動作
- ⚠️ **条件付き合格**: Step 1～5は正常だが、Google Sheets反映に問題
- ❌ **不合格**: Step 1～5のいずれかで期待結果と異なる動作

---

## 🧪 テストケース2: パターンC（ユーザー拒否）

### 目的

未登録店舗が存在する場合に、ユーザーがChatGPT分類を拒否したとき、警告メッセージと未登録店舗リストが表示されることを確認する。

### テストデータ準備

#### 1. マッピングデータの事前登録

マッピング管理画面で以下の店舗のみ登録（一部の店舗を意図的に未登録にする）：

| 店舗名 | マッチタイプ | カテゴリ | 列 | 優先度 |
|-------|------------|---------|-----|--------|
| イオン | contains | 日用品費 | D列 | 1 |

#### 2. テスト用CSVファイル作成

`test_pattern_c.csv`（Shift_JIS、内容例）:

```csv
ご利用日,ご利用店名,ご利用金額
260115,イオン,1500
260116,セブンイレブン,500
260117,ローソン,300
260118,未登録店舗ABC,1000
```

### テスト手順

#### Step 1～4: パターンBと同様

1. トップ画面にアクセス
2. CSVファイルアップロード（`test_pattern_c.csv`）
3. プレビュー表示確認
4. 設定確認

#### Step 5: 取込実行（未登録店舗検出）

1. 「取込実行」ボタンをクリック
2. 確認ダイアログで「OK」をクリック
3. **期待結果**:
   - プログレスバーが表示される
   - バックエンド処理（`POST /process`）が実行される
   - `/process`のレスポンス:
     ```json
     {
       "status": "needs_classification",
       "unregistered_count": 3,
       "unregistered_stores": [
         {"store_name": "セブンイレブン", "amount": 500, "count": 1},
         {"store_name": "ローソン", "amount": 300, "count": 1},
         {"store_name": "未登録店舗ABC", "amount": 1000, "count": 1}
       ]
     }
     ```
   - プログレスバーが非表示になる

#### Step 6: 確認ダイアログ表示

1. **期待結果**:
   - JavaScript `confirm()`ダイアログが表示される
   - メッセージ:
     ```
     未登録店舗が3件あります。
     ChatGPTで自動分類しますか？

     ※OpenAI APIキーが設定されている必要があります
     ```

#### Step 7: ユーザー拒否

1. 「キャンセル」ボタンをクリック
2. **期待結果**:
   - JavaScript `alert()`ダイアログが表示される
   - メッセージ: 「未登録店舗があります。マッピング管理画面で登録してください。」

3. 「OK」をクリック

#### Step 8: 未登録店舗リスト表示確認

1. **期待結果**:
   - Step 5: 処理結果エリアは表示される（または表示されない）
   - ページ上に警告メッセージが表示される:
     - アイコン: 警告三角形（`bi-exclamation-triangle-fill`）
     - 背景色: 黄色（`alert-warning`）
   - 未登録店舗リストが表示される:
     ```
     - セブンイレブン: 1件、500円
     - ローソン: 1件、300円
     - 未登録店舗ABC: 1件、1,000円
     ```
   - 「マッピング管理画面で登録」ボタンが表示される

#### Step 9: マッピング管理画面へのリンク確認

1. 「マッピング管理画面で登録」ボタンをクリック
2. **期待結果**:
   - マッピング管理画面（`/mapping`）にリダイレクトされる
   - マッピング一覧が表示される
   - 新規追加フォームが表示される

#### Step 10: Google Sheets確認（未反映確認）

1. Googleスプレッドシートを開く
2. 2025年シートを選択
3. **期待結果**:
   - **データが反映されていない**（パターンCは拒否により処理中断）
   - または、イオン（登録済み店舗）のみ反映、未登録店舗は未反映

### 判定基準

- ✅ **合格**: すべてのステップが期待結果通りに動作
- ⚠️ **条件付き合格**: 警告表示は正常だが、リスト表示に軽微な問題
- ❌ **不合格**: ユーザー拒否時の動作が期待結果と異なる

---

## 🔍 共通確認項目

### セキュリティ確認

#### CSRF保護

1. **確認方法**:
   - ブラウザの開発者ツール（F12）を開く
   - ネットワークタブでPOSTリクエストを確認
   - `POST /process`のリクエストヘッダーに`X-CSRF-Token`が含まれているか確認

2. **期待結果**:
   ```
   X-CSRF-Token: <トークン値>
   ```

#### セッション管理

1. **確認方法**:
   - ブラウザのCookieを確認
   - `server_session_id`というCookieが存在するか確認

2. **期待結果**:
   - Cookieサイズが小さい（32バイト程度のUUID）
   - 大容量データはSessionStoreに保存されている（ブラウザCookieには含まれない）

### パフォーマンス確認

#### 処理時間測定

1. **確認方法**:
   - 「取込実行」ボタンをクリックしてからリダイレクトまでの時間を測定

2. **期待結果**:
   - 100件以下: 10秒以内
   - 1000件: 30秒以内

### UI/UX確認

#### レスポンシブデザイン

1. **確認方法**:
   - ブラウザウィンドウをリサイズ
   - スマートフォンビューで表示

2. **期待結果**:
   - テーブルが横スクロール可能（`table-responsive`）
   - ボタンが押しやすいサイズ
   - テキストが読みやすい

#### エラーメッセージ表示

1. **確認方法**:
   - 意図的にエラーを発生させる（例: スプレッドシートIDを空にする）

2. **期待結果**:
   - Bootstrap Toastでエラーメッセージが表示される
   - 背景色: 赤（`text-bg-danger`）
   - アイコン: エラーマーク
   - メッセージ内容が明確

---

## 📊 テスト結果記録表

### パターンB（未登録なし）

| Step | 項目 | 結果 | 備考 |
|------|-----|------|------|
| 1 | 初期画面表示 |  |  |
| 2 | CSVアップロード |  |  |
| 3 | プレビュー表示 |  |  |
| 4 | 設定確認 |  |  |
| 5 | 取込実行 |  |  |
| 6 | 成功メッセージ |  |  |
| 7 | Google Sheets確認 |  |  |

**総合判定**: ⬜ 合格 / ⬜ 条件付き合格 / ⬜ 不合格

### パターンC（ユーザー拒否）

| Step | 項目 | 結果 | 備考 |
|------|-----|------|------|
| 1～4 | 初期フロー |  |  |
| 5 | 取込実行 |  |  |
| 6 | 確認ダイアログ |  |  |
| 7 | ユーザー拒否 |  |  |
| 8 | 未登録店舗リスト |  |  |
| 9 | マッピング管理画面リンク |  |  |
| 10 | Google Sheets確認 |  |  |

**総合判定**: ⬜ 合格 / ⬜ 条件付き合格 / ⬜ 不合格

---

## 📝 テスト実施時の注意事項

### Docker環境の確認

実行前に以下を確認：

```bash
# Docker起動確認
docker ps

# ログ確認
docker logs aeon-card-import-system --tail 50

# エラーがある場合は再起動
docker-compose restart
```

### テストデータのクリーンアップ

各テスト前に以下を実施：

1. Googleスプレッドシートの該当セルをクリア
2. ブラウザのCookieをクリア（必要に応じて）
3. マッピングデータを初期状態に戻す

### トラブルシューティング

#### プレビューが表示されない

**原因**: セッションストアの問題
**対処**:
```bash
docker exec -it aeon-card-import-system rm -f /app/data/sessions/sessions.db
docker-compose restart
```

#### Google Sheets APIエラー

**原因**: サービスアカウント認証の問題
**対処**:
1. `config/service_account.json`が配置されているか確認
2. スプレッドシートの共有設定を確認
3. サービスアカウントメールに編集権限を付与

#### CSSが適用されない

**原因**: 静的ファイルの配信問題
**対処**:
```bash
# Nginxキャッシュクリア
docker-compose restart
```

---

## 🎯 テスト完了条件

### 合格基準

- ✅ パターンBが期待通りに動作（7/7ステップ合格）
- ✅ パターンCが期待通りに動作（10/10ステップ合格）
- ✅ セキュリティ確認項目がすべて合格
- ✅ パフォーマンス要件を満たす

### 不合格基準

- ❌ パターンBまたはCのいずれかで重大なエラーが発生
- ❌ Google Sheetsへの反映が不正確
- ❌ セキュリティ要件を満たさない

### 条件付き合格基準

- ⚠️ 軽微なUI表示の問題（機能は正常）
- ⚠️ パフォーマンス要件を若干下回る（大幅ではない）

---

**作成者**: Claude Code (Sonnet 4.5)
**作成日時**: 2026-02-04
**テスト実施者**: 未実施
**テスト実施日時**: 未実施
**バージョン**: v2.0（ChatGPT分類機能）
